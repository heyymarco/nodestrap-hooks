List => add numbered variant
Offcanvas*****
fix modalCard close btn

Dropdown => role => allActionCtrl = 'menu', else = 'dialog' && aria-modal=true
Carousel => infinite loop
Navbar => use List

Media => for profile image, video, static images, gifs, svgs, etc
Progress
Range
fix focus-visible