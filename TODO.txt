top priority:

^\s*border((Start|End){0,2})Radius?(Start|End)?\s*:(?!\s*(any|borderRadiusRefs\.border\1Radius))

[DONE] config padding(Inline|Block) => expandPadding
[PROG] config border(Start|End){0,2}Radius => expandBorderRadius
config borderWidth(Inline|Block) => expandBorderStroke
ModalSide => enabled/disabled problem

<hr > => change backg to border-top
ListSeparatorItem => border = inherit container

check Content::usesBorderRadius() & Content::usesPadding() parameters
test ActionControl/Button => click on disabled state
Navbar => use List
[['calc(0px -', cssProps.paddingBlock, ')']] => `${}`
Button => implements AnchorHTMLAttributes
Input => implement InputHTMLAttributes



medium priority:
Carousel => infinite loop



low priority:
Media => for profile image, video, static images, gifs, svgs, etc
Progress
Range
fix focus-visible
Check_togglerBtn & NavbarMenu fix blinky toggle active => press for a few seconds and then released, the blinky occured
List:variant_tab => move up tab border radius from ListItem to Wrapper
List:breadcrumb => fix next icon color