urgent priority:
sync VisuallyHidden

fallbacks:
sync Input
sync Navbar

// layouts: => // styles:   from './cssfn'

composition => style
* @returns A `[Factory<StyleCollection>, ReadonlyRefs, ReadonlyDecls]` => * @returns A `[Factory<Rule>, ReadonlyRefs, ReadonlyDecls]`
* @returns A `Rule`

mainComposition([        =>   mainComposition(
compositionOf('foo', [   =>   compositionOf('foo',
globalDef([              =>   globalDef(
(?<!rule|fromEntries|imports|rules|variants|states|>)\(\[


top priority:
fix indicator togglerActiveReducer
add Gatsby & Remix support on ActionControl
add client side Form for Remix
tweak useMemo in Element
move Check::onChange, Range::onChange to EditableControl::onChange
fix Button press={props.press ?? active} => press={props.press ?? (active || undefined)}
remove nude variant on Check, Label, Popup, & Range
replace substr to substring or slice



medium priority:
fix Carousel fast clicking
Media => for profile image, video, static images, gifs, svgs, etc
Range => dial variant



low priority:
css-config => filter valid props
fix focus-visible



stucked:
Navbar => use List
[stucked] fix overflow detection Navbar padding-end
[stucked] List:variant_tab => move up tab border radius from ListItem to Wrapper
